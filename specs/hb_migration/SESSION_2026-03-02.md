# 会话记录 2026-03-02

## 今日完成事项

### 一、Hyperliquid Spot 市场调研（已结案）

| 结论 | 详情 |
|------|------|
| BTC-USDC 不存在 | Hyperliquid Spot 只有原生代币，BTC 仅在 Perp 市场 |
| HYPE-USDC 深度误判 | 24h 成交额仅 $207，订单簿静态深度大但实际无成交 |
| PURR-USDC 最优 Spot | 24h 成交 $578K，但流动性仍属薄弱 |
| **决策：放弃 Spot，改做 Perps** | Hyperliquid Perp 市场（BTC/ETH）流动性充足 |

---

### 二、Base Uniswap V3 LP 手工冒烟测试（Step 1~G 全部完成）

| Step | 名称 | 结果 | 备注 |
|------|------|------|------|
| A | 配置核对 | ✅ PASS | yml + 脚本默认值全部合规 |
| B | 本地回归测试 | ✅ PASS | 17/17 全绿（wp3 + p0） |
| C | Gateway + 钱包检查 | ✅ PASS | Gateway v2.12.0，Base Block #42,827,690 |
| D | 小额 LP 策略启动 | ✅ PASS | 修复连接器名称后成功启动 |
| E | 日志与 adapter 输出 | ✅ PASS | 所有字段完整，JSONL 正常写入 |
| F | gas 不足拦截演练 | ✅ PASS | `gas_reserve_insufficient` 拦截生效 |
| G | 安全停止 | ✅ PASS | 两次 `stop command initiated` 日志正常 |

---

### 三、本次修复的 Bug

| 文件 | 问题 | 修复方式 |
|------|------|---------|
| `scripts/base_vol_adaptive_lp.py` | 连接器名 `uniswap/clmm/base` Hummingbot 不识别 | 改为 `uniswap/clmm`，网络由 Gateway defaultNetwork 决定 |
| `gateway-files/conf/connectors/uniswap.yml` | 缺少 `defaultNetwork` 字段导致 `gateway balance` 报错 | 添加 `defaultNetwork: base` |
| `conf/scripts/conf_base_vol_adaptive_lp_0.yml` | entry_amount 超过当前钱包余额 | 临时下调 entry_amount（测试用，待充值后恢复） |
| `start` 脚本（历史遗留） | 路径含 `handongcui`，conda 未安装 | 已在上次会话修复，使用 `.venv310` |

---

### 四、风控机制验证结果

| 机制 | 触发条件 | 验证结果 |
|------|---------|---------|
| `gas_reserve_insufficient` | ETH < min_gas_reserve | ✅ 正确拦截 |
| `max_position_pct_exceeded` | 开仓额 / 总资产 > 50% | ✅ 正确拦截 |
| adapter 事件输出 | 每次 tick 决策 | ✅ 字段完整，JSONL 可审计 |

---

## 当前钱包状态（冒烟测试结束时）

- 地址：`0x834B1efB29ba2301B44549a249D921fE9dA14795`（Base 主网）
- ETH：0.0145（⚠️ 不足，需补充）
- USDC：18.4945（⚠️ 不足 25 目标值）
- WETH：0.0096（⚠️ 略低于 0.01）
- Hyperliquid USDC：$19.80

---

## 待办事项（优先级排序）

### 🔴 高优先级（进入实盘前必须完成）

- [ ] **充值 Base 钱包**
  - ETH 补充至 ≥ 0.035（开仓 gas + min_gas_reserve 0.02 双重保障）
  - USDC 补充至 ≥ 30
  - 方式：[bridge.base.org](https://bridge.base.org) 或直接转账至 `0x834B...795`

- [ ] **恢复 entry_amount 至原始值**（充值后执行）
  ```yaml
  entry_amount_eth: 0.01
  entry_amount_usdc: 25
  min_gas_reserve: 0.02
  ```

- [ ] **配置 Hyperliquid Perps 策略**
  - 清理 `.env` 中 Spot 配置段
  - 配置 `hyperliquid_perpetual` 连接器
  - 选定交易对（BTC-USD 或 ETH-USD）
  - 复用 FGI + LLM 决策逻辑，适配合约策略

### 🟡 中优先级

- [ ] **修复 `asyncio.TimeoutError`**
  - 症状：Gateway 单次请求偶发超时
  - 方向：检查 Base RPC 端点响应速度，可换备用 RPC（如 Alchemy/QuickNode）

- [ ] **`max_position_pct` 逻辑复核**
  - 当前触发原因是总资产估算方式，需确认分母是否包含 USDC + WETH 市值
  - 充值后重新验证是否正常放行

- [ ] **补充 AGENTS.md SOP**
  - 记录 `gateway connect ethereum` 正确用法
  - 记录 `gateway balance ethereum WETH,USDC` 正确用法
  - 记录 Gateway keychain 解锁步骤

### 🟢 低优先级

- [ ] **Hyperliquid Spot 结论归档**
  - 在 AGENTS.md 中记录：Spot 只适合 PURR-USDC，其余对流动性不足
  - 更新 `.env` 注释，明确标注已放弃 Spot

- [ ] **运行完整 CI 测试套件**
  - 目前只跑了 2 个脚本测试，建议跑 `pytest test/ -v` 全量

---

## 明日建议首要任务

1. 充值 → 验证余额达标
2. 正式启动 LP 策略（真实开仓 0.008 ETH + 15 USDC）
3. 观察 30 分钟，验证开仓日志 + adapter 字段（含 `range_lower/upper`、`in_range`）
4. 开始配置 Hyperliquid Perps 策略

---

## 环境快速参考

```bash
# 启动 Hummingbot
cd /Users/darrencui/hummingbot && ./start

# 启动 Gateway（如容器未运行）
cd /Users/darrencui/hummingbot && docker-compose --profile gateway up -d gateway

# 如 SSH 会话 keychain 报错，先解锁
security -v unlock-keychain ~/Library/Keychains/login.keychain-db

# 查询 Base 链余额
# 在 Hummingbot >>> 内：
gateway balance ethereum WETH,USDC

# 启动策略
start --script base_vol_adaptive_lp.py --conf conf_base_vol_adaptive_lp_0.yml

# 查看策略日志
tail -f /Users/darrencui/hummingbot/logs/logs_conf_base_vol_adaptive_lp_0.log

# 查看 adapter 事件
tail -f /Users/darrencui/hummingbot/logs/base_vol_adaptive_lp_events.jsonl
```
